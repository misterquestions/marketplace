import { Typography } from '@mui/material';

const Footer = () => {
  return (
    <Typography align="center" sx={{ marginTop: 'calc(5% + 1rem)', bottom: 0 }}>
      &copy; 2021 - Created by Fernando Lopez
    </Typography>
  );
};

export default Footer;
