import LoginButton from "../../account/LoginButton";

const NavigationUserAccount: React.FC = () => {
  // Todo: Implement session verification and display user account navigation
  // Currently, this is just a placeholder that shows the login/register links
  return (
    <LoginButton />
  );
};

export default NavigationUserAccount;
